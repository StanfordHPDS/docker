FROM stanfordhpds/python-uv-ssh:latest

# The ONBUILD instructions in python-uv-ssh will:
# 1. Copy pyproject.toml, uv.lock, .python-version (if present)
# 2. Install the Python version required
# 3. Create virtual environment and install dependencies
# 4. Copy the rest of the project files